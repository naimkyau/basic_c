#include <stdio.h>

int main()
{
    FILE *ptr;
    ptr = fopen("naim.txt", "w");

    // // fgetc
    // char c;
    // c = fgetc(ptr);
    // printf("%c\n", c);

    // //fputc
    // char c;
    // fputc('N', ptr);
    return 0;
}