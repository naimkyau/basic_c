// Don't run this program it is infinite
// Don't run this program it is infinite
// Don't run this program it is infinite

#include <stdio.h>
int main()
{
    int i = 0;
    while (i < 10)
    {
        printf("The value of i is %d\n", i);
        i++;
    }
    return 0;
}